{
  "name": "agent-scheduler",
  "version": "1.0.0",
  "description": "Agent Scheduler - Cron-like scheduling for agent tasks",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "example": "ts-node examples/index.ts"
  },
  "keywords": ["scheduler", "cron", "agent", "jobs"],
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.2"
  }
}
